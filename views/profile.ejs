<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Message Board</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet"  href="/styles/styles.css">
    <link rel="stylesheet"  href="/styles/profile.css">
</head>
<body>
    <%- include('partials/headerLoggedIn') %>
    <main>
        <div class="form-container">
            <h1>Your Profile</h1>
            <% if (errors.length > 0) { %>
                <div class="server-form-error">
                    <% errors.forEach(function(error) { %>
                        <p><%= error.msg %></p>
                    <% }); %>
                </div>
            <% } %>
            <form  method="POST" id="update-form">
                <div class="form-group">
                    <label for="username">Username</label>
                    <input 
                        type="text" 
                        id="username" 
                        name="username" 
                        class="form-input username" 
                        placeholder="e.g coolMessager" 
                        value="<%= formData ? formData.username : '' %>"
                    >
                    <div class="error-container username-err"></div>
                </div>
                <div class="form-group">
                    <label for="email">Email</label>
                    <input 
                        type="text" 
                        id="email" 
                        name="email" 
                        class="form-input email" 
                        placeholder="e.g email@email.com" 
                        value="<%= formData ? formData.email : '' %>"
                    >
                    <div class="error-container email-err"></div>
                </div>
                <input type="hidden" name="formSource" value="profileUpdate">
                <button type="submit" class="btn">Update profile</button>
            </form>
        </div>
        <div class="secret-container form-container">
            <h2>Know the secret?</h2>
            <p class="secret-desc">With the secret key you will be able to see other users names! Do you know what it is?</p>
            <form method="POST" action="/users/secretSubmit">
                <div class="form-group">
                    <label for="secret">Secret Key</label>
                    <input 
                        type="text" 
                        id="secret" 
                        name="secret" 
                        class="form-input secret" 
                        placeholder="e.g Secret Message" 
                        required
                    >
                </div>
                <button type="submit" class="btn">Submit</button>
            </form>
        </div>
    </main>
    <%- include('partials/footer') %>
    <script src="/js/update-validation.js"></script>
</body>
</html>